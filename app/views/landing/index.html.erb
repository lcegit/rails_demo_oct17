<%= link_to 'new article', '/articles/new' %>

<ul>
  <% @articles.each do |article| %>
    <li>
      <em><%= link_to article.title, article_path(article) %></em><br>
      <%= article.content %>
    </li>
  <% end %>
</ul>
